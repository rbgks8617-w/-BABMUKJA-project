# Asset optimization notes

- `assets/tuk-campus-map.png` is still bundled because the campus map screen depends on it. Before Android/iOS release, compress it or convert it to WebP and verify map labels and building touch targets on device.
